<div class="max-w-3xl">
    {% if widget.title %}
        <h2 class="mb-2 text-sm font-semibold text-zinc-800 dark:text-zinc-200">{{ widget.title }}</h2>
    {% endif %}
    <div class="whitespace-pre-line text-sm leading-relaxed text-zinc-600 dark:text-zinc-400">{{ widget.body }}</div>
</div>
