{% extends 'admin/layout.twig' %}

{% block doc_title %}{{ title }} — Admin{% endblock %}

{% block heading %}{{ title }}{% endblock %}

{% block header_actions %}
    {% import 'admin/partials/icon_svg.twig' as icons %}
    <a href="{{ route('admin.resource.create', { slug: slug }) }}" class="inline-flex items-center gap-2 rounded-lg border border-zinc-600/50 bg-zinc-100 px-3.5 py-2 text-sm font-medium text-zinc-950 shadow-sm ring-1 ring-zinc-800/40 transition-colors hover:bg-white">
        {{ icons.r('plus', 'h-4 w-4 shrink-0 text-zinc-700') }}
        <span>Create</span>
    </a>
{% endblock %}

{% block body %}
{% import 'admin/partials/icon_svg.twig' as icons %}
<div class="mx-auto max-w-7xl space-y-5">
    {% set flash = session_flash('admin_success') %}
    {% if flash %}
        <p class="flex items-center gap-2 rounded-lg border border-emerald-500/30 bg-emerald-500/5 px-4 py-3 text-sm text-emerald-100/90 ring-1 ring-emerald-500/20">
            {{ icons.r('check', 'h-4 w-4 shrink-0 text-emerald-400/90') }}
            <span>{{ flash }}</span>
        </p>
    {% endif %}

    {% if tableFilters|length > 0 or perPageOptions|length > 1 %}
        <form method="get" action="{{ route('admin.resource.index', { slug: slug }) }}" class="adm-filter-panel flex flex-wrap items-end gap-4 rounded-xl border border-zinc-700/80 bg-zinc-900/30 p-4 ring-1 ring-zinc-800/70">
            <div class="mb-1 flex w-full items-center gap-2 border-b border-zinc-800/70 pb-3 text-xs font-semibold uppercase tracking-wide text-zinc-500">
                {{ icons.r('funnel', 'h-4 w-4 shrink-0 text-zinc-500') }}
                <span>Filters</span>
            </div>
            {% if tableSort.persist %}
                <input type="hidden" name="sort" value="{{ tableSort.key }}">
                <input type="hidden" name="sort_dir" value="{{ tableSort.dir }}">
            {% endif %}
            {% for filter in tableFilters %}
                <div class="flex min-w-[10rem] flex-col gap-1">
                    <label class="text-xs font-medium text-zinc-500" for="adm_f_{{ attribute(filter, 'name') }}">{{ attribute(filter, 'label') }}</label>
                    {% set fp = filter.queryParam %}
                    {% if filter.inputKind == 'select' %}
                        <select id="adm_f_{{ attribute(filter, 'name') }}" name="{{ fp }}" class="rounded-lg border border-zinc-700 bg-zinc-950 px-2.5 py-2 text-sm text-zinc-100 shadow-sm focus:border-zinc-500 focus:outline-none focus:ring-2 focus:ring-zinc-500/30">
                            <option value="">— All —</option>
                            {% for optVal, optLabel in filter.options %}
                                <option value="{{ optVal }}" {% if attribute(filterValues, fp) == optVal %}selected{% endif %}>{{ optLabel }}</option>
                            {% endfor %}
                        </select>
                    {% else %}
                        <input id="adm_f_{{ attribute(filter, 'name') }}" type="text" name="{{ fp }}" value="{{ attribute(filterValues, fp)|default('') }}" class="rounded-lg border border-zinc-700 bg-zinc-950 px-2.5 py-2 text-sm text-zinc-100 placeholder:text-zinc-600 shadow-sm focus:border-zinc-500 focus:outline-none focus:ring-2 focus:ring-zinc-500/30" autocomplete="off">
                    {% endif %}
                </div>
            {% endfor %}
            {% if perPageOptions|length > 1 %}
                <div class="flex min-w-[10rem] flex-col gap-1">
                    <label class="text-xs font-medium text-zinc-500" for="adm_per_page">Per page</label>
                    <select id="adm_per_page" name="per_page" class="rounded-lg border border-zinc-700 bg-zinc-950 px-2.5 py-2 text-sm text-zinc-100 shadow-sm focus:border-zinc-500 focus:outline-none focus:ring-2 focus:ring-zinc-500/30" aria-label="Rows per page" onchange="this.form.submit()">
                        {% for n in perPageOptions %}
                            <option value="{{ n }}" {% if n == tablePerPageCurrent %}selected{% endif %}>{{ n }}</option>
                        {% endfor %}
                    </select>
                </div>
            {% endif %}
            {% if tableFilters|length > 0 %}
                <div class="ml-auto flex items-center gap-2">
                    <button type="submit" class="inline-flex items-center gap-1.5 rounded-lg border border-zinc-600/40 bg-zinc-100 px-3.5 py-2 text-sm font-medium text-zinc-950 shadow-sm ring-1 ring-zinc-800/30 hover:bg-white">
                        {{ icons.r('check', 'h-4 w-4 shrink-0 text-zinc-700') }}
                        <span>Apply</span>
                    </button>
                    <a href="{{ route('admin.resource.index', { slug: slug }) }}" class="inline-flex items-center gap-1.5 rounded-lg border border-zinc-700 bg-transparent px-3.5 py-2 text-sm font-medium text-zinc-200 ring-1 ring-zinc-800/40 hover:bg-zinc-900/60">
                        {{ icons.r('arrow-path', 'h-4 w-4 shrink-0 text-zinc-500') }}
                        <span>Reset</span>
                    </a>
                </div>
            {% endif %}
        </form>
    {% endif %}

    {% if tableColumnPickerEnabled %}
        <div class="relative flex flex-wrap items-center justify-end gap-2" style="z-index:50">
            <button type="button" id="adm_col_picker_toggle" class="inline-flex items-center gap-2 rounded-lg border border-zinc-700 bg-zinc-900/40 px-3 py-2 text-sm font-medium text-zinc-200 shadow-sm ring-1 ring-zinc-800/60 hover:bg-zinc-900/70 focus:outline-none focus-visible:ring-2 focus-visible:ring-zinc-500/40" aria-expanded="false" aria-controls="adm_col_picker_panel" aria-haspopup="true">
                {{ icons.r('columns', 'h-4 w-4 shrink-0 text-zinc-500') }}
                <span>Columns</span>
                {{ icons.r('chevron-down', 'h-4 w-4 shrink-0 text-zinc-500') }}
            </button>
            <div id="adm_col_picker_panel" class="absolute right-0 mt-2 min-w-[14rem] rounded-xl border border-zinc-700/90 bg-zinc-950 p-3 shadow-xl ring-1 ring-zinc-800/80" style="z-index:51;top:100%" hidden role="region" aria-label="Column visibility">
                <p class="mb-2 flex items-center gap-2 text-xs font-medium uppercase tracking-wide text-zinc-500">
                    {{ icons.r('columns', 'h-4 w-4 shrink-0 text-zinc-600') }}
                    <span>Visible columns</span>
                </p>
                <div id="adm_col_picker_list" class="flex max-h-64 flex-col gap-2 overflow-y-auto text-sm"></div>
                <p class="mt-3 text-xs text-zinc-600">Saved in this browser only.</p>
            </div>
        </div>
    {% endif %}

    <div id="adm_resource_table" class="adm-resource-frame overflow-hidden rounded-xl border border-zinc-700/80 bg-zinc-900/10 shadow-sm ring-1 ring-zinc-800/70">
        {% if records is empty %}
            <p class="flex flex-col items-center justify-center gap-3 px-4 py-14 text-center text-sm text-zinc-500">
                {{ icons.r('table', 'h-7 w-7 shrink-0 text-zinc-600 opacity-80') }}
                <span>{{ tableEmptyMessage|default('No records found.') }}</span>
            </p>
        {% else %}
            <div class="overflow-x-auto">
                <table class="adm-table-grid w-full border-collapse text-sm">
                    <thead>
                    <tr>
                        {% for col in tableColumns %}
                            <th data-adm-col="{{ col.name }}" class="border-b border-zinc-800 bg-zinc-900/70 px-4 py-3 text-left text-xs font-semibold uppercase tracking-wide text-zinc-400">
                                {% if col.sortUrl is defined %}
                                    <a href="{{ col.sortUrl }}" class="inline-flex items-center gap-1 rounded text-zinc-400 transition-colors hover:text-zinc-100 focus:outline-none focus-visible:ring-2 focus-visible:ring-zinc-500/40">
                                        <span>{{ col.label }}</span>
                                        {% if col.sortActive|default(false) %}
                                            {% if col.sortDir == 'desc' %}
                                                {{ icons.r('chevron-down', 'h-4 w-4 shrink-0 text-zinc-500') }}
                                            {% else %}
                                                {{ icons.r('chevron-up', 'h-4 w-4 shrink-0 text-zinc-500') }}
                                            {% endif %}
                                        {% endif %}
                                    </a>
                                {% else %}
                                    {{ col.label }}
                                {% endif %}
                            </th>
                        {% endfor %}
                        {% if tableActions|length > 0 %}
                            <th data-adm-col="__actions" class="border-b border-zinc-800 bg-zinc-900/70 px-4 py-3 text-right text-xs font-semibold uppercase tracking-wide text-zinc-400" aria-label="Actions"></th>
                        {% endif %}
                    </tr>
                    </thead>
                    <tbody class="bg-zinc-950/30">
                    {% for row in records %}
                        {% set rowIndex = loop.index0 %}
                        <tr class="border-b border-zinc-800/80 transition-colors last:border-0 hover:bg-zinc-900/35">
                            {% for col in tableColumns %}
                                {% set cell = attribute(row, col.name) %}
                                <td data-adm-col="{{ col.name }}" class="px-4 py-3 text-sm text-zinc-200 {% if col.kind in ['text', 'numeric', 'email', 'url', 'datetime', 'color', 'toggle', 'image'] %}whitespace-nowrap{% endif %}">
                                    {% include 'admin/resource/cells/' ~ col.kind ~ '.twig' with { col: col, cell: cell } only %}
                                </td>
                            {% endfor %}
                            {% if tableActions|length > 0 %}
                                <td data-adm-col="__actions" class="whitespace-nowrap px-4 py-3 text-right">
                                    {% for act in tableRowActions[rowIndex] %}
                                        {% if act.kind == 'link' %}
                                            <a class="mr-3 inline-flex items-center gap-1.5 text-sm text-zinc-300 underline decoration-zinc-600 underline-offset-2 transition-colors hover:text-white" href="{{ route(act.route, act.routeParams) }}">
                                                {{ icons.r('pencil', 'h-4 w-4 shrink-0 text-zinc-500') }}
                                                <span>{{ act.label }}</span>
                                            </a>
                                        {% else %}
                                            <form method="post" action="{{ route(act.route, act.routeParams) }}" class="inline-flex items-center" data-adm-confirm="Delete this record? This cannot be undone.">
                                                <input type="hidden" name="_csrf" value="{{ csrfToken }}">
                                                <button type="submit" class="inline-flex cursor-pointer items-center gap-1.5 border-0 bg-transparent p-0 text-sm text-red-400/95 underline decoration-red-500/50 underline-offset-2 hover:text-red-300">
                                                    {{ icons.r('trash', 'h-4 w-4 shrink-0 text-red-400') }}
                                                    <span>{{ act.label }}</span>
                                                </button>
                                            </form>
                                        {% endif %}
                                    {% endfor %}
                                </td>
                            {% endif %}
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        {% endif %}
    </div>

    {% if pagination.hasPages %}
        <nav class="flex flex-wrap items-center justify-between gap-4 rounded-lg border border-zinc-800/80 bg-zinc-900/20 px-4 py-4 ring-1 ring-zinc-800/60" aria-label="Pagination">
            <span class="text-sm text-zinc-500">Page {{ pagination.page }} of {{ pagination.last_page }} · {{ pagination.total }} total</span>
            <div class="flex flex-wrap items-center gap-2">
                {% if not pagination.onFirstPage %}
                    <a href="{{ pagination.urlForPage(pagination.page - 1) }}" class="inline-flex items-center gap-1.5 rounded-lg border border-zinc-700 bg-zinc-900/30 px-3.5 py-2 text-sm font-medium text-zinc-200 ring-1 ring-zinc-800/50 hover:bg-zinc-900/60">
                        {{ icons.r('chev-left', 'h-4 w-4 shrink-0 text-zinc-500') }}
                        <span>Previous</span>
                    </a>
                {% endif %}
                {% if not pagination.onLastPage %}
                    <a href="{{ pagination.urlForPage(pagination.page + 1) }}" class="inline-flex items-center gap-1.5 rounded-lg border border-zinc-700 bg-zinc-900/30 px-3.5 py-2 text-sm font-medium text-zinc-200 ring-1 ring-zinc-800/50 hover:bg-zinc-900/60">
                        <span>Next</span>
                        {{ icons.r('chev-right', 'h-4 w-4 shrink-0 text-zinc-500') }}
                    </a>
                {% endif %}
            </div>
        </nav>
    {% endif %}

    <script type="application/json" id="adm-table-config" data-enabled="{{ tableColumnPickerEnabled ? '1' : '0' }}" data-slug="{{ slug }}">{{ tableColumnPickerMetaJson|raw }}</script>
</div>
{% endblock %}
